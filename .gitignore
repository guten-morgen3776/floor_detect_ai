# Kaggle input data (train, valid, test 等の大量データはプッシュしない)
kaggle/input/
cubicasa5k_processed/
scripts/my_cubicasa_dataset/
my_cubicasa_dataset/
cubicasa5k_pix2pix/
cubicasa_segment/

# 大量データ・アーカイブ（pushしない）
archive*/
archive (1)/
*.zip

# モデルの重みファイルを除外
*.pth